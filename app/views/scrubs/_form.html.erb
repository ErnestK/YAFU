<%= form_for(@scrub) do |f| %>
  <% if @scrub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scrub.errors.count, "error") %> prohibited this scrub from being saved:</h2>

      <ul>
      <% @scrub.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :file_type %><br>
    <%= f.text_field :file_type %>
  </div>
  <div class="field">
    <%= f.label :size %><br>
    <%= f.number_field :size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
